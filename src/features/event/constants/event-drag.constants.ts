/**
 * Drag & Drop constants for Event feature
 * Defines origin names and drop targets for events
 */
export const EVENT_DRAG_CONSTANTS = {
  ORIGIN: 'event',
  DROP_TARGETS: {
    EVENT: 'event',
    TRASH: 'trash',
  },
} as const
