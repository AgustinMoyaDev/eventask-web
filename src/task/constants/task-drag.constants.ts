/**
 * Drag & Drop constants for Task feature
 * Defines origin names and drop targets for tasks
 */
export const TASK_DRAG_CONSTANTS = {
  ORIGIN: 'task',
  DROP_TARGET: 'event', // Tasks can be dropped on events
} as const
