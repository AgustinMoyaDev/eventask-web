# Copy this file to .env and fill in your values

# API Configuration
VITE_API_URL=http://localhost:4000/api
VITE_BACKEND_URL=http://localhost:4000

# Google OAuth Client ID
# Get yours from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here

# Development Notes:
# - MSW (Mock Service Worker) is enabled automatically in development mode
# - Run `pnpm dev` to start development server with mocks
# - Run `pnpm build` for production build (no mocks)
# - Run `pnpm build:demo` for demo build (with mocks)